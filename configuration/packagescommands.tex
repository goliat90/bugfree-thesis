\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}

\usepackage{ae} %almost european" fonts package
\usepackage{amsmath} %math
\usepackage{icomma} %intelligent commas for decimal numbers
\usepackage{units} %typesetting of units, fractions.
%color package, can colour text for example, (color for draft and finished?)
\usepackage{color} 
%enchanced graphics. (include and such)
\usepackage{graphicx}
%converts eps files to pdf for you, allows you to include eps files instead.
\usepackage{epstopdf}
%figure environment can have additional subfigures. Changed to the newer
%package subfig, subfigure is deprecated, subfig is backwards compatible.
\usepackage{subfig}
%\usepackage{subfigure}
%blackboard fonts, contains symbols related to math amongs things.
\usepackage{bbm}
%caption for floating environments, figures, tables...
\usepackage{caption}
%flexible bibliography support, extends bibtex,
%since im switching to biber comment out.
%\usepackage[square, numbers, sort]{natbib}

%improves the tabular environment with cells that span multiple rows.
\usepackage{multirow}
%extends options for tabular and array environments
\usepackage{array}
%customize page layout.
\usepackage{geometry}
%control layout of headers and footers.
\usepackage{fancyhdr}
%customization of chapter heading, also has predefined styles.
\usepackage{fncychap}
%url for adding urls/emails.
\usepackage[hyphens]{url}
%dropped letter commands, when the first letter in the word in
%the paragraph is larger than the line.
\usepackage{lettrine}
%add picture commands/backgrounds to all pages.
\usepackage{eso-pic}

%added this because of waring during compilation
\usepackage{csquotes}

%enables adding todonotes in the document.
\usepackage{todonotes}

%hyperlinks in texts and such.
\usepackage[breaklinks,pdfpagelabels=false]{hyperref}

%bibliography biblatex (follow up of bibtex), using IEEE style
\usepackage[style=ieee,backend=biber]{biblatex}
%adding reference file
\addbibresource{references.bib}


%\newcommand{\rd}{\ensuremath{\mathrm{d}}}
%\newcommand{\id}{\ensuremath{\,\rd}}
%\newcommand{\degC}{\ensuremath{\,\unit{^\circ C}}}
%\newcommand{\mail}[1]{\href{mailto:#1}{\nolinkurl{#1}}}

% Fancyheader shortcuts
\newcommand{\setdefaulthdr}{%
\fancyhead[L]{\slshape \rightmark}%
\fancyhead[R]{\slshape \leftmark}%
\fancyfoot[C]{\thepage}%
}
\newcommand{\setspecialhdr}{%
\fancyhead[L]{ }%
\fancyhead[R]{\slshape \leftmark}%
\fancyfoot[C]{\thepage}%
}
%
\newcommand{\backgroundpic}[3]{%
	\put(#1,#2){
		\parbox[b][\paperheight]{\paperwidth}{%
			\centering
			\includegraphics[width=\paperwidth,height=\paperheight,keepaspectratio]{#3}
			\vfill
}}}



